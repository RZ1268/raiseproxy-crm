# Raiseproxy CRM

Cloud deployment source for Raiseproxy CRM + Outreach.

> Public repository contains code only. Customer data, user credentials, SMTP passwords, and production secrets must never be committed here.
