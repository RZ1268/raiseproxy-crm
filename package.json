{
  "name": "raiseproxy-crm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node build.js",
    "dev": "node build.js && wrangler dev",
    "deploy": "node build.js && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
